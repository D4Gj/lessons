# функцию isPrime, которая принимает число и возвращает простое число или нет
# Простое число - число которое делится на 1.txt и на само себя
def more_than_zero(x):
    if x > 0:
        return True
    else:
        return False

def isPrime():
    for
        if
            return False
    return True

zero_check = lambda x: True if x > 0 else False
